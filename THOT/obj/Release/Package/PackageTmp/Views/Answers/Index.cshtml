@model IEnumerable<THOT.Models.Answer>

@{
    ViewBag.Title = "Respuestas";
}

<h2>Respuestas</h2>

@*<p>
    @Html.ActionLink("Agregar Respuesta", "Create")
</p>*@
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.Question.Description)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsCorrect)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.Question.Description)
        </td>*@
        <td>
            @Html.Raw(item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsCorrect)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.AnswerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AnswerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AnswerId })*@
            <a href="@Url.Action("Edit", new { id = item.AnswerId })">
                <i class="glyphicon glyphicon-pencil"></i>
                <span class="sr-only">Edit</span>
            </a>|
            <a href="@Url.Action("Delete", new { id = item.AnswerId })">
                <i class="glyphicon glyphicon-trash"></i>
                <span class="sr-only">Delete</span>
            </a>
        </td>
    </tr>
}

</table>

<p>
    <a href="@Url.Action("Index","Questions")">
        <i class="glyphicon glyphicon-circle-arrow-left"></i>
        <span class="sr-only">Back</span>
    </a>
</p>
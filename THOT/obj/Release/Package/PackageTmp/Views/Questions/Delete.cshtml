@model THOT.Models.Question

@{
    ViewBag.Title = "Eliminar Pregunta";
}

<h2>Eliminar</h2>

<div>
    <h4>Pregunta</h4>
    <h5>¿Estás seguro de eliminar este elemento?</h5>
    <hr />
    <dl class="dl-horizontal">
        @*<dt>
            @Html.DisplayNameFor(model => model.Test.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Test.Name)
        </dd>*@

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.Raw(Model.Description)
        </dd>

        <dd>
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <input type="submit" value="Eliminar" class="btn btn-default" />
            }
        </dd>

    </dl>

    <div class="form-actions no-color">
        @*@Html.ActionLink("Back to List", "Index")*@
        <div>
            <a href="@Url.Action("TestQuestions", new { id = Model.TestId })">
                <i class="glyphicon glyphicon-circle-arrow-left"></i>
                <span class="sr-only">Back</span>
            </a>
        </div>
    </div>

</div>
